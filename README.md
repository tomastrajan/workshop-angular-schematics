# Angular Shematics Workshop


## Useful commands

* `schematics .:<schematic-name>` - run schematics with `schematics-name` from the current project (`.`), the `:` is a separator between the schematics package and scheamtics name
